# Automatically added by dh_pycentral
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall libft-python
	if grep -qs '^libft-python$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^libft-python$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
